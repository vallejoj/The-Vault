'use strict';
module.exports = function() {
  var daKey;
  var daValue;
    function setValue(key, value){
    daKey=key
    daValue=value
}
    function getValue(key){
if (daKeys) {
  return daValue
}
    }
    return{key:key,
    value:value}
};
